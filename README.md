# Monolith

**At this point the language is at its conception stage. Nothing works and nothing is expected to do so. This message will be removed when at least a minimal code example can be successfully compiled.**

`carta.mn` is the file where the language is defined. It serves both as a tutorial and a code sample that the compiler should be able to compile.

The current bootstrap compiler is written in TypeScript.
